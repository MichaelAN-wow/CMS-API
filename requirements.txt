# --- All third-party libs used in model_creat.py ---
# Stdlib (no install): os, sys, base64, socket, warnings, logging, asyncio

# Web framework (FastAPI, uvicorn)
fastapi
uvicorn
python-multipart

# Validation (often bundled with FastAPI)
pydantic

# Image / ML
numpy
opencv-python-headless
insightface
onnxruntime

# Time sync (NTP)
ntplib
